"""The ONESHOT port: the DT2 voice record, the adapter's arithmetic, a baked
sample bank, original test samples, and the DN2 section 7 that carries them
(`docs/dt2-machine-port.md`). The transplanted render itself is `dnfw.transplant`.
"""
